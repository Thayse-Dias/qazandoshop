Projeto de Automação de Testes - QazandoShop

📋 Índice

- Descrição do Projeto

- Pré-requisitos

- Instalação

- Estrutura do Projeto

- Configuração do Cypress

- Comandos Personalizados

- Execução dos Testes

- Explicação do Código

- Solução de Problemas

🚀 Descrição do Projeto

Este projeto realiza testes automatizados end-to-end no site Automation Practice utilizando o framework Cypress. Os testes cobrem dois fluxos principais:

- Cadastro de usuário

- Login e adição de produtos aos favoritos

⚙️ Pré-requisitos

Antes de começar, certifique-se de ter instalado em sua máquina:

- Node.js (versão 14 ou superior)

- npm (geralmente vem com o Node.js)

- Git (para controle de versão)

Verificando as instalações:
```bash
node --version
npm --version
git --version
```
📥 Instalação

1. Clone o repositório ou crie uma nova pasta para o projeto
```bash
mkdir automação-qazando
cd automação-qazando
```
2. Inicialize um novo projeto Node.js
```bash 
npm init -y
```
3. Instale o Cypress
```bash
npm install cypress --save-dev
```
4. Abra o Cypress pela primeira vez para gerar a estrutura de pastas
```bash
npx cypress open
```
- Isso criará a estrutura padrão de pastas do Cypress e arquivos de configuração.


Comando: fazerLogin(email, senha)

Fluxo:

- Visita a página inicial

- Clica no link de login

- Preenche o campo de usuário

- Preenche o campo de senha

- Clica no botão de submit

- Confirma o modal de sucesso

Parâmetros:

email: Email do usuário

senha: Senha do usuário

Comando: navegarEFavoritar()

Fluxo:

- Abre o menu lateral (offside-menu)

- Navega para a loja (menuShopText)

- Acessa a página de favoritos (favoritesPage)

- Adiciona o primeiro item aos favoritos

- Abre o offcanvas do carrinho

- Realiza três verificações de sucesso

Verificações:

- Mensagem de sucesso: Busca por textos indicativos no body

- Contador de itens: Verifica se há pelo menos 1 item na lista

- Elemento do carrinho: Confirma que o botão do carrinho existe no DOM

🗂️ Estrutura do Projeto:

A estrutura do projeto deve ser semelhante a esta
```bash
automação-qazando/
├── cypress/
│   ├── e2e/
│   │   └── main.cy.js          # Arquivo principal de testes
│   ├── support/
│   │   ├── commands.js         # Comandos personalizados
│   │   └── e2e.js             # Arquivo de suporte
│   └── downloads/              # Downloads durante testes
│   └── fixtures/               # Dados de teste
│   └── screenshots/            # Screenshots de falhas
├── node_modules/
├── cypress.config.js           # Configuração do Cypress
└── package.json
```

🔄 Próximos Passos Sugeridos

- Adicionar mais casos de teste

- Implementar Page Objects

- Adicionar testes de API

- Configurar CI/CD

- Adicionar relatórios de teste

- Implementar dados dinâmicos com fixtures